{
  "name": "suss-ai",
  "version": "1.0.0",
  "description": "SUSS AI Social Lie Detector",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "build": "cd backend && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 